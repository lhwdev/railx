# Compatibility API for RailX

Although railx jar itself provides sufficient api for third party addons to integrate, this provides more utility to
use that works regardless of whether railx mod is loaded.
